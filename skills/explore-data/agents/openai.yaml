interface:
  display_name: "Explore data"
  short_description: "Inspect grain, joins, coverage, and usable comparisons"
  default_prompt: "Use $explore-data with the question and artifacts I provide."
policy:
  allow_implicit_invocation: true
