interface:
  display_name: "Plan an analysis"
  short_description: "Choose comparisons and tests before seeing the answer"
  default_prompt: "Use $plan-analysis with the question and artifacts I provide."
policy:
  allow_implicit_invocation: true
