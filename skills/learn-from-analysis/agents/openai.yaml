interface:
  display_name: "Learn from analysis"
  short_description: "Preserve scoped lessons with evidence for the next task"
  default_prompt: "Use $learn-from-analysis with the question and artifacts I provide."
policy:
  allow_implicit_invocation: true
