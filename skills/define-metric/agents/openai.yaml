interface:
  display_name: "Define a metric"
  short_description: "Resolve counting rules before a metric becomes a target"
  default_prompt: "Use $define-metric with the question and artifacts I provide."
policy:
  allow_implicit_invocation: true
