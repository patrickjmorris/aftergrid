# Issue tracker

Issues for aftergrid live in **GitHub Issues** on `patrickjmorris/aftergrid`, via the `gh` CLI.

Triage labels use the five canonical roles with default names: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`.

PRs as a request surface: off.
