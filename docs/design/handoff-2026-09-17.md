# Handoff, 2026-09-17 (session TopazJaguar / CrimsonLotus)

Context for whoever picks the beads up next. Delete this file when the beads it names are closed (bead
`ag-handoff-doc-emm`). Owner: Patrick Morris. Working rules that stood all session: stage commits by explicit
path, never `git add -A`; commit trailer lines per the session reminder; secrets never in tracked files; nothing
reported as verified, approved, executed or ready unless it was; do not touch `site/`, `assets/`,
`docs/design/landing-page-directions.md`, `docs/inputs/*` (owner's in-progress files); `br` only in the main
checkout, never in worktrees; worktree agents merge `main` before their final commit.

## State at handoff

- `main` at 4822802, CI green (seam-1 on ubuntu+macos × Node 22.18/24, pack-smoke, examples-check), 527 tests.
- Every agent-able bead closed. Open beads are owner gates (db2, a2f, rdo, the two PR approvals), things
  blocked on them (91e, 4zn, demo .5/.6/.7), v0.1 (ogy), and the follow-ups filed today (below).
- Two real Findings on open data are merged into `examples/nyc-open-data/analytics/findings/`:
  `2026-09-17-crz-trips-after-pricing` (revision 2, `inconclusive` by its own pre-registered falsifier; PR #2,
  head 1b9aeb2) and `2026-09-17-member-ebike-share-jan2025` (`answered`, member e-bike share 62.8% → 69.2%;
  PR #3, head bf22ff4). Both are `complete`, evidence valid, rerun reproduces, agent-reviewed, **not approved**:
  readiness is `not_ready` until the owner submits an APPROVED review on each merged PR and an attestation is
  committed (`ag-demo-attest-ready-8st`).
- `examples/nyc-open-data/docs/run-log.md` records all six `/analyze` runs, what broke, and what each taught.
  `examples/nyc-open-data/README.md` is the new-user walkthrough. `docs/design/fanout-review-2026-09-16.md` is
  the review record for the engine beads.

## How the work was done

Implementation in git worktrees by Opus agents (one bead each), then an adversarial review agent (two lenses,
refute-your-own-findings), a fix agent with fail-before/pass-after tests, and for larger changes an independent
re-verification against the reviewer's repros. Merge to `main`, full suite, push, CI watch, close the bead with a
comment naming the commits. Bounded reviews by CobaltSpring (codex) via agent mail on request.

Headless `/analyze` runs: from the Instance root, `claude --plugin-dir <copy of the plugin OUTSIDE the repo>
--permission-mode acceptEdits --allowedTools "Bash(aftergrid:*)" … -p "/analyze …"`, with `analytics/golden/`
moved out of the Instance for the duration (it is the answer key) and restored after. Loading the plugin from the
repo itself makes every write inside the repo "a sensitive file" (Claude Code protects the loaded plugin's tree).
The CLI is on PATH through a one-line shim to `bin/aftergrid.mjs`. All runs used the owner's Max subscription;
the `total_cost_usd` in the JSON envelope is an estimate at API list price, not a charge.

## Decisions taken this session (owner's, unless marked)

- Demo lives in `examples/nyc-open-data/` (not a separate repo), public from day one, datasets TLC + NOAA GHCN +
  Citi Bike. Windowed Instance pattern: `scripts/build-data.mjs` (bounded tables, provenance, `--append`) and
  `scripts/derive-question-tables.mjs` (`crz_daily`, because `trips_daily` exceeds the capture cap).
- Congestion Finding: reframe weekday/weekend as separate Claims (revision 2) rather than accept the run-2 halt.
- Citi Bike runs on Opus. Model choice per run is the owner's; cost lever noted in the run log.
- Actions setting "Allow GitHub Actions to create and approve pull requests" enabled so `examples-pr.yml` can open
  Finding PRs as `github-actions[bot]` (the contract forbids the approver authoring the PR).
- Contract changes landed from the runs (agent decisions, reviewed): a fired pre-registered falsifier is an
  `inconclusive` outcome, never an evidence error; pre-registration pins SQL hash + required + expected_outcome +
  statement hash; named derived-value operands (`after/baseline`, `minuend/subtrahend`, `numerator/denominator`);
  `review status` ends the chain and exits non-zero on stale; superseded reviews are history; capture admission
  per table; `permission_denied` halt state; counter-metrics on definitions (owner's feature, contract first).

## Bead map for the next agent (ids as of 2026-09-17)

Owner first: approve PR #2 and PR #3 → `ag-demo-attest-ready-8st`. Decide `ag-nightly-api-key-p7d` (API key in
CI or the model path stays unexercised) → then `ag-demo-open-data-qsl.7`.

Ready for an agent, no owner input needed: `ag-eval-reference-only-kq5`, `ag-demo-golden-scorecard-wr0`,
`ag-skills-unused-capture-dqy`, `ag-review-agent-identity-x06`, `ag-demo-runlog-flags-mwm`.

Owner or a human on a fresh machine: `ag-demo-walkthrough-timing-zz4`, then db2 / a2f / rdo (real Finding,
Reader session), then 91e, 4zn.

## Known limits, stated

- No model-in-the-loop eval has run in CI; the nightly's command template is probe-verified, never executed.
- The counter-metric rule binds values at the column; a subpopulation row of the right column is the reviewer's
  check, not the Engine's (`docs/contracts/finding-manifest.md`).
- `demo.duckdb` is local only (builder 2.0.0, four months); the walkthrough rebuilds it.
- The Citi Bike Finding's committed render predates the superseded-review relabel; re-render before publishing.
